class HttpMethods {
    constructor() {
        this.POST = `post`;
        this.GET = `get`;
        this.PUT = `put`;
        this.DELETE = `del`;
        this.CONNECT = `connect`;
        this.PATCH = `patch`;
        this.OPTIONS = `options`;
        this.HEAD = `head`;
    }
}

export default new HttpMethods();
