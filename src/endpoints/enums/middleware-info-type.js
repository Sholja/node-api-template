class ResponseFormatType {
    constructor() {
        this.SCHEMA_VALIDATOR = `SCHEMA_VALIDATOR`;
        this.VALIDATOR = `VALIDATOR`;
        this.EXECUTOR = `EXECUTOR`;
        this.AUTH = `AUTH`;
    }
}

export default new ResponseFormatType();
